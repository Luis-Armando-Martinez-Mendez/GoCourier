@model GoCourier.Domain.Entities.Notificacion

@{
    ViewData["Title"] = "Eliminar Notificación";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-danger">
            <i class="bi bi-trash-fill"></i> Eliminar Notificación
        </h2>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left-circle"></i> Volver a la lista
        </a>
    </div>

    <div class="card border-danger shadow-sm rounded">
        <div class="card-body">
            <h5 class="text-danger fw-bold">¿Estás seguro que deseas eliminar esta notificación?</h5>
            <dl class="row">
                <dt class="col-sm-3 fw-bold text-secondary">Usuario</dt>
                <dd class="col-sm-9">@Model.Usuario?.Email</dd>

                <dt class="col-sm-3 fw-bold text-secondary">@Html.DisplayNameFor(model => model.Mensaje)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Mensaje)</dd>

                <dt class="col-sm-3 fw-bold text-secondary">@Html.DisplayNameFor(model => model.Fecha)</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Fecha)</dd>
            </dl>
        </div>
     </div>

    <div class="mt-4 d-flex justify-content-end">
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger me-2">
                <i class="bi bi-trash"></i> Eliminar
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left-circle"></i> Cancelar
            </a>
        </form>
    </div>
</div>